# 帝王三國繁體版 — 协议逆向总纲

> 最后更新：2026-09-13
> 数据来源：`帝王三國繁體版.apk` → `classes.dex` 反编译 + HttpCanary 真机抓包 + 318/328 区实测
> 工具：`apk_king/dex_probe.py`（纯 Python DEX 解析）、`apk_king/dex_disasm.py`（Dalvik 反汇编）
> 完整操作码映射：`apk_king/_ops.json`（138 条）

---

## 一、★ 核心突破：两套发包结构（2026-09-13）

**这是之前 318 区一直返回空的根本原因。登录和业务操作用的是完全不同的两套结构，不能混用。**

### 1.1 业务操作包（正确写法）

```
writeUTF(版本`ctype`channelId)     ← 2B 长度前缀 + 23B 内容
long    时间戳ms
byte    包数量 = 1
long    playId
long    0
short   数据长度 = len(数据体) - 2      ★ 注意减 2
short   op
bytes   数据体（明文，不加密）           ★ 关键
```

要点：
- **数据体不加密**（之前误用登录那套 `encrypt()`，这是最致命的错）
- **不带 md5 签名**（登录才需要）
- `Content-Type: application/x-www-form-urlencoded`
- 长度字段写的是 `len(数据体) - 2`，不是实际长度

对应实现：`山贼筛选_WebUI_v2.py` 的 `_game_post_raw()` / `send_op_raw()`

### 1.2 登录包（旧结构，保持不动）

```
writeUTF(header) + long(时间戳) + byte(包数)
每包：long(playId) + long(0) + short(len) + short(op) + writeUTF(md5签名) + 加密数据体
```

md5 签名算法：`md5(str(op + 时间戳 + playId) + SALT)`，`SALT = "FIC1atsuf30"`

不加密白名单：`NO_ENC_OPS = {4096, 4097, 4098, 4099, 4101, 4118}`

对应实现：`_game_post()` / `send_op()`。登录、心跳（25215）走这条路，实测正常。

### 1.3 抓包定位过程（避免重犯）

两个真实包逐字节 diff 才定位到字段位置：

| 偏移 | 包1 | 包2 | 结论 |
|---|---|---|---|
| @0..7 | `0001a098abecff01` | `0001a098abefde01` | 递增 → **时间戳** |
| @9..16 | `3fe6af58c8724854` | 同 | 不变 → **playId** |

之前把这两个字段搞反，导致签名和加密全建立在错误前提上。

### 1.4 环境常量

```
LOGIN_URL    = http://pass.dwsg.gameme5.com:8192/common/area/list.action
VALIDATE_URL = http://pass.dwsg.gameme5.com:8192/system/user/validate.action
GAME_PATH    = /kingWapServer/HttpClient
C_VERSION    = 1630107      ★ 抓包实证（曾误用 1640516）
C_TYPE       = 7054
CHANNEL_ID   = 0200000000
SALT         = FIC1atsuf30
MAP_W, MAP_H = 187, 56
gamekey      = \x00\x17 + "1630107`7054`0200000000"
```

响应偏移铁律：**`op_response = op_request + 0x7000`**

---

## 二、★ 山贼功能（已打通）

### 2.1 5440 reqThiefList — 普通山贼（1~10 级）

```
请求 op 5440 → 响应 op 34112
数据体：short(0) + short(x) + short(y)      共 6 字节，明文
```

第一个 short 恒为 0（328 区实测填 1 也返回相同结果，服务端忽略）。

响应结构（源自 `p.l1()` 反汇编）：
```
short  地图宽 = 187
short  地图高 = 56
byte   数量 N
N × {  long(ID) + UTF(名称) + short(等级) + byte + ...
       将领数组 + 兵种数组 + 掉落描述UTF  }
```

实测：318 区中心 (91,26) 单点返回 22 只，2 圈 25 点去重 61 只。

### 2.2 8195 — 智擒主帅（14~15 级，固定 5 条）

```
请求 op 8195 → 响应 op 36867
数据体：任意（被忽略，客户端发 new byte[1]）
```

响应格式与 5440 **完全相同**，可复用同一个解析器。实测 318 区返回 5 条「1星主帥/2星主帥」，等级 14~15，兵种含投石车、重弩车。

### 2.3 5442 reqResourceList — 资源点

```
请求 op 5442 → 响应 op 34114
数据体：short(x) + short(y)      与 5440 构造完全一致（p.h1 vs p.k1）
```

**后续加功能可直接复用山贼的代码路径**，只改 op 和解析。

### 2.4 UI 层调度逻辑（`p.A0(type, x, y)`）

```java
p.C = x;  p.D = y;              // 存静态字段
type == 0 → h1(x,y)  → 5442 资源点
type == 1 → h1(x,y)  → 5442
type == 2 → k1(x,y)  → 5440 山贼
```

坐标来源：`p.c0`/`p.d0` 是两个输入框文本 → `isDigital` 校验 → `intValue` → 与 `p.g3`/`p.h3`（地图宽高）比上限。

**结论：坐标可以随便填，不需要那里有城池。** 服务端以该点为中心返回周边山贼。

`p.B` 是纯客户端界面状态，**不上行**，所以不存在服务端侧的「打开界面」前置包。

### 2.5 相关操作码

| op | 请求名 | 说明 |
|---|---|---|
| 25221 / 25222 | `reqAutoHitbanditSet` | 自动消灭山贼的战报过滤设置 |
| 5419 | `reqDetect` | 探测，数据体 `long + byte + long` |
| 5421 | `reqDetectConsume` | 探测消耗 |

---

## 三、★ 后续可加功能（按可行性排序）

### 3.1 立即可做（数据体结构已确认，复用现有代码）

| 功能 | op | 数据体 | 备注 |
|---|---|---|---|
| **资源点筛选** | 5442 | `short(x) + short(y)` | 与山贼完全同构，最容易加 |
| **主帅筛选** | 8195 | 任意 | 响应格式同山贼，解析器直接复用 |
| **城池列表** | 5490 | `int` | 实测返回 5939B，可解出全区城池坐标 |
| **敌国城池** | 5472 | — | `reqEnemyCountryCityList` |
| **我的封地** | 5474 / 4880 | — | `reqRoleFiefList` |

资源点这个最推荐先做 —— 改 3 行就行（op 5440→5442、响应 34112→34114、解析字段微调）。

### 3.2 需要额外逆向响应格式

| 功能 | op | 数据体结构（已确认） |
|---|---|---|
| **出征打山贼** | 5410 | `byte(类型) + byte(n) + long[n](将领ID) + long(目标) + long(目标2) + byte×3` |
| 出征预提示 | 5408 | `byte + byte(n) + long[n] + long` |
| 将领配兵 | 4646 | `long(将领ID) + byte(兵种) + short(数量) + int` |
| 士兵平均招募 | 4680 | `long + int×4` |
| 军队操作 | 5632 | `byte(7) + long + short + short` |
| 配兵筛选 | 5634 | `byte + short + short` |
| 副本开始 | 6459 | `short + int + boolean` |
| 关卡选择 | 6408 | `int` |

**「筛选 → 自动出征打山贼」是最有价值的组合**：5440 拿列表 → 4646 配兵 → 5408 预提示 → 5410 出征。数据体全部已知，只需逆向战报响应。

### 3.3 其它可能有用的模块

| 分类 | 操作码 | 请求名 |
|---|---|---|
| 将领 | 4629 / 4644 / 4650 / 4706 | `reqLoadGenerals` / `generalAddPoint` / `reqGeneralUseItem` / `reqGeneralSkillCtrl` |
| 封地 | 4678 / 4624 / 4655 | `reqFiefInfo` / `fiefMoveAbleCitys` / `fiefForceExpel` |
| 城池 | 4864 / 4868 / 4902 | `reqCityFiefList` / `reqCityGarrisonList` / `reqCityResetTax` |
| 国家 | 5128~5206 | 捐献/联盟/军情/改名等 20+ 个 |
| 竞技场 | 25186~25200 | `ArenaTopListMsg` / `ArenaBattle` / `ArenaTeamStrategy` |
| 户部种植 | 25380 / 25384 | `REQ_HUBU_PLANT_PICK` / `REQ_HUBU_PLANT_BAT` |
| 帅府战法 | 25601~25610 | 激活/学习/升级/分解战法 |
| 签到/活动 | 25088 / 25090 | `houyedu` / `qiandao`（实测能返回任务列表 1404B） |
| 副本 | 6450~6460 | `reqFBGroups` / `reqFBJoinTeam` / `reqFBStart` / `reqFBRecords` |
| 转服 | 6433~6440 | `reqShiftServerPreview` / `reqShiftserverCommit` |
| 邮件/客服 | 4374 / 4376 / 25136~25143 | 收发消息、客服工单 |

---

## 四、借鉴项目（sanguohx.exe）逆向状态

**结论：山贼功能无法参考，团子喵这边已经走得更远。**

`qingting-master/_deliverables/` 下 15 份文档全文搜 `5440 / reqThiefList / 山贼 / thief`，**只命中 1 处**（账号目录里的 `记录山贼.txt` 文件名）。

原因见 `一键启动_账号文件.md`：

> game_login 每 ~5s 无限重试，但从不出现它之后的 `game_key` / `serveraddress` / `go_game` 请求。
> 一键启动的「线程 spawn 成功」≠「真正进游戏」。根因是缺真实游戏服地址。

那次逆向卡在进游戏之前，**从未抓到山贼包**。sanguohx 被 Nuitka 编译，文档也写明「字段在方法体内运行时赋值，Nuitka 内联，无法从签名读出」。

有价值的遗留成果：
- `game_packet_protocol.md` — gamekey 生成格式（已验证正确）
- `protocol_spec.md` — 渠道地址表
- 账号配置 INI 格式（如果以后要做多账号管理可参考）

而借鉴项目当年缺的「真实区服地址 + 进游戏」，这边已打通（318 区 `enter_game` 返回登錄成功）。

---

## 五、逆向工具与方法（可复用）

### 5.1 自研工具

容器内没有 jadx/baksmali/java，团子喵自己写了：

- `apk_king/dex_probe.py` — DEX 解析：字符串池、类型、方法签名、字段、class_data 遍历
- `apk_king/dex_disasm.py` — Dalvik 反汇编：invoke-*、const-*、sget/sput、iget/iput、if-*、new-array 等

### 5.2 挖新操作码的标准流程

1. **定位发包函数**：`s/a->z(short, byte[])` 是最终发包出口（`method_idx=41958`）
2. **扫调用点**：全 dex 找 invoke 指令的 method_idx 匹配，得到 191 个发包方法
3. **提取 op + 请求名**：方法内 `openDos("请求名")` 的字符串参数 + 之后的 `const/16` 值
4. **读数据体结构**：按顺序列出方法内所有 `BaseIO.write*` 调用序列
5. **找响应解析器**：全 dex 搜 `const/16 = op+0x7000`（signed），落在 `a0/a.g5()` 的分发表里，再跳到对应的 `p.xx()` 解析方法

### 5.3 教训

- **不要只靠猜参数格式**：团子喵试了 40+ 种数据体组合、6 种前置包、扫了 3 个操作码段，全部无效。真正的问题在发包结构（加密+签名），不在参数。
- **不要用低等级号做对照**：328 区那号 <30 级走新手随机机制，服务端对畸形包宽松处理，返回全图随机 8 只 —— 这个假信号误导了很久。
- **抓包 > 猜测**：两个真实包的逐字节 diff 直接终结了问题。

---

## 六、当前成品

**`apk_king/山贼筛选_WebUI_v2.py`** — 单文件零依赖 WebUI

```
【协议层】常量 / 取区服列表 / 匹配区服 / 会话探活 / 检查踢下线
         KingClient: enter_game(4099) / game_login(4100)
                     send_op()      ← 登录用（加密+签名）
                     send_op_raw()  ← 业务用（明文+无签名）★
【会话层】SESSION 常驻 / 心跳线程(25215) / 标记掉线 / 尝试重连
【业务层】ring_points(圈层就近搜索) / 功能_取山贼数据 / 队伍统计 / 兵种摘要 / 是否符合
【HTTP 层】GET / /status /session ｜ POST /login /logout /check /autorelogin /start /stop /areas
```

实测：318 区 61 只（2圈25点），328 区 8 只，9 组筛选条件全部正确。

---

## 五、角色信息操作码（已实测打通）

响应 op = 请求 op + **28672 (0x7000)**

| 用途 | 请求 op | 响应 op | 返回数据结构（大端） |
|---|---|---|---|
| 进入游戏 | 4099 loginBaseinfo | 32771 | 状态串 + 角色ID + **角色名** + **国家名** |
| 游戏登录 | 4100 gameLogin | 32772 | 全量快照（含角色名、城池、战法列表） |
| 等级/经验 | 4108 | 32780 | `[4B]` + `等级(1B)` + `经验(8B)` + `升级经验(8B)` |
| **角色资源** | **4128** | **32800** | 见下表 ★ |
| 加点(含角色状态) | 5128 reqPointAdd | 33800 | `fe 00 00` + 铜钱(8B) + 粮食(8B) + 0(8B) + 等级(8B) + 经验(8B) + 升级经验(8B) |
| 资源点列表 | 5442 reqResourceList | 34114 | `short(x)+short(y)` 请求 |
| 山贼列表 | 5440 | 34112 | — |

### ★ op 4128 响应结构（固定 56 字节）

| 偏移 | 宽度 | 字段 | 示例值 |
|---|---|---|---|
| 0  | 8B BE | 铜钱 copper | 143426 |
| 8  | 8B BE | 粮食 food | 194161 |
| 16 | 4B BE | 人口上限 popMax | 1368 |
| 20 | 4B BE | 人口已用 popUsed | 1193 |
| 24 | 8B BE | 资源点当前 resCur | 200 |
| 32 | 8B BE | 资源点上限 resMax | 515 |
| 40 | 8B BE | 宝藏当前 treasureCur | 0 |
| 48 | 8B BE | 宝藏上限 treasureMax | 0 |

### 前端契约（商辅 AccountsView 逆向）

轮询 `GET /api/bot/{id}/poll?sinceIndex=N` 的 `data` 必须包含：

```
status          账号状态 0=停止 1=运行中 2=启动中 5=需验证
lastError       错误信息
lastAccessTime  最后访问时间
source          数据来源
character       {charName, level, kingdom, copper, food,
                 popUsed, popMax, resCur, resMax,
                 treasureCur, treasureMax, yellowTurbCount, dungeonCount}
generals        武将数组
officers/troops/items/roleStatuses/convoyCountries
logs            字符串数组（增量，前端 I.value.push(...logs)，保留最近 50 条）
total / nextIndex  日志总条数（前端 S.value = total）
```

**常见坑**：`character` 为 `null` 时前端不渲染角色面板；`logs` 必须是**字符串数组**（不是对象数组）；`total` 用于下一次 `sinceIndex`。

---

## 六、APK 逆向成果（DEX 分析，权威）

**方法**：反编译 `classes.dex`（8.8MB），用 androguard 4.1.4 提取
（坑：`d.get_strings()` 返回**纯 str 列表**；`const-string` 的 `get_output()` 是
`v1, "reqRoleInfo"` 这种**字符串值**格式，不是索引）

**产出**：`操作码表.json` — 141 条「命令名 → 操作码」高置信度映射
脚本：`apk_work/dex_cmdmap2.py`（生成）、`apk_work/dump_method.py`（dump 指定方法）

### 6.1 op 4128 响应结构（客户端解析器 `LscriptPages/data/f;->w`）

| 偏移 | 读取 | 客户端字段 | 含义 | 实测样例 |
|---|---|---|---|---|
| 0  | readLong | `data/f;->d`  | 铜钱 | 144,144 |
| 8  | readLong | `data/f;->e`  | 粮食 | 194,787 |
| 16 | readInt  | `data/f;->m`  | 人口上限 | 1368 |
| 20 | readInt  | `data/f;->n`  | 人口已用 | 1193 |
| 24 | readLong | `data/f;->q`  | 未确认 | 200 |
| 32 | readLong | `data/f;->r`  | 未确认 | 515 |
| 40 | readLong | `s/a;->Ga`    | 黄金 | 0 |
| 48 | readLong | `s/a;->Ha`    | 白银 | 0 |

**8+8+4+4+8+8+8+8 = 56 字节** ✓ 与实测包长完全一致

### 6.2 角色名 / 等级 / 国家（op 4100 gameLogin 响应）

- **君主名**：包内第一条中文串（2字节长度前缀 + UTF-8），形如 `00 09 <嗷嗷嗷>`
- **等级**：紧跟在角色名之后的**1 个字节**（`07` = 7 级）★ 关键，容易取错位
- **国家**：从 op 4099(loginBaseinfo) 响应取第 3 条中文串（顺序：状态串 / 角色名 / 国家名）

### 6.3 「资源点」不是单一字段

客户端 UI（`LscriptPages/gameHD/k;->i`）里「资源点」与
`产钱 / 产粮 / 封地 / 城池 / 将领` 并列，属于**封地类型分类**，
需从封地列表统计，**不在 op 4128 里**。待继续确认。

### 6.4 已知响应 op 换算

**响应 op = 请求 op + 28672 (0x7000)**，响应 op 以有符号 short 表示
（如 4128 → 32800 → -32736）
